{
  "entries": {
    "app/main.js": {
      "maxFiles": 119,
      "targetFiles": 74,
      "maxSourceBytes": 1460343,
      "targetSourceBytes": 750000,
      "reason": "The application initial-static closure includes the background-owned StoredOptionsV4 snapshot client with optimistic revision conflicts and sparse official overrides, bilingual official-rules view and safety copy, committed-reset cleanup warning recovery in core state, plus the pre-document browser-session restore guard, URL-bound opening-claim handoff, one-shot prompt handoff layout and page orchestration, per-frame send queue, DNR-ready iframe assignment, authenticated frame identity, navigation-generation-isolated content bridge recovery, same-target navigation de-duplication, opaque black new-topic masking, canonical Notion frame-load URL comparison, prompt-focus restoration after iframe navigation unless a typed modal is open, one post-layout frame before the first embedded navigation, measured image-plus-text composer sizing, Grok Cookie preflight, Notion navigation safeguards, model settlement with a per-panel interaction-blocking overlay, structured primary, secondary and add-on attempt rows, focus ownership and Toast collision avoidance, same-conversation Grok and Notion response URL preservation, five-minute session-scoped secondary fallback memory restored before the first overlay render, accessible Workspace appearance subtabs, workspace reconciliation, persistent user settings with timeout-bound coalesced retry, owner-scoped iframe tab context menus with complete tab operations, prompt-history date-group labels, the page-local remembered ChatClub-tab sidebar with empty current-tab close, drag-resize, left-side tab count, header close-others cleanup, overlay rename/delete with Appearance-owned hover-button folding, last-viewed/edited/created plus name/open sorting with prompt-history date groups, folder create and drag membership, closed-tab divider, on-row title editing, 1-based indexes, remembered Tabs export sanitizer, and first-send topic-title generation; signed-rules controls remain behind explicit lazy boundaries while this shared runtime is held to its exact footprint; Share panel state-port joins the initial-static closure while the Share controller stays lazy; History panel state-port joins the initial-static closure while the History controller stays lazy; right-click Pocket/History topbar save captures the current workspace immediately without waiting for idle full-text; English UI labels DouBao as 豆包（doubao） and Qianwen as 千问（qianwen）; the shared Summary catalog now includes the Manus message-extraction site; GET_CONFIG_SNAPSHOT declares INVALID_OFFICIAL_RULES_CATALOG; Prompt History matching now NFKC-normalizes extracted USER turns so live and stored conversations still match after punctuation or CJK spacing drift; History right-click injects the captured workspace turns into the lazy viewer and distinguishes added versus refreshed prompts; typed modal() now carries dialog semantics, scroll lock, Tab containment, restore-focus, and sibling inert without native dialog, plus dual toast live regions and linear-menu Tab dismiss; leftover window.confirm call sites now open themed confirmationModal via openConfirmationAction; favicon fallback glyphs use system-ui instead of Inter; confirmation chrome now carries danger/warning/neutral tone, dialog-vs-alertdialog roles, outlined neutral danger actions, and undo toasts for reversible list-item deletes; Grok rid-only iframe URL matching and Delete content-bridge ping/recovery with parent postMessage bindFrame fallback join that same-conversation Grok URL preservation; a registration wait timeout no longer poison-reloads a painted iframe, and ChatClub Tabs falls back from a stale live tabId to open; New Chat preserve now freezes the workspace from durable conversation snapshots, including when the active Notion pane is already on /ai; a rebound desk with a real topic title stays rememberable after reload even when iframe hrefs are still home, and a lagged chrome URL claims the rebound binding instead of resurrecting a detached hash; the claim response may declare reboundFromStaleUrl so that lagged hash is not rejected by the page contract; runtime bootstrap failures include the underlying claim cause; New Chat marks each resetting frame as having released its conversation (frame-loading newChatPending) so the rebound workspace id is first persisted at the app home and conversation retention cannot resurrect the frozen thread from a stale iframe src after a browser restart. Desk auto-naming adds the workspace auto-title controller to the bootstrap closure: a desk whose conversations were opened inside the frames (site sidebar, Pocket restore, History) is named from the site-published document title with the brand stripped, or from the first user message through the same topic-title generation as a composer prompt, on a bounded probe schedule that stops for New Chat, workspace rebinding, frame navigation, and titled desks. Empty-home Delete All Topics now disables the topbar trash from workspace.hasDeletableActiveThreads so Grok/Kagi/Notion homepages no longer toast skipped chats. Empty-home Summary now disables the topbar Summary control from workspace.hasSummarizableActiveThreads so those same start pages do not open a collector. Delete availability follows conversation identity, so Grok /chat without an id stays undeletable. Empty-home Share now disables the topbar Share control from workspace.hasShareableActiveThreads so those start pages do not open a capture panel. Empty-home New Chat All now disables the topbar New Chat control from workspace.hasNewChatActiveThreads so a home-only desk is not frozen. Current models assignment, API provider model catalogs, and per-slot outbound model keys join that exact bootstrap footprint. Bilingual API-key reveal and catalog fallback-model copy join the shared i18n table in that same closure. Appearance compact click-reorder is a boolean option and html dataset that hides Settings list Move up / Move down by default. Shared favicon candidate walking, network DuckDuckGo+Google auto-fetch, and per-app icon overrides join the bootstrap closure. API provider rows reuse the shared favicon primitive with skip-catalog href/logoUrl so vendor hosts do not inherit chat-app icons. Network lookup now walks api/ai/www parent hosts and origin /favicon.ico so API endpoints like api.0-0.pro still resolve a brand mark. Favicon images keep a pending empty-tile until decode, and Settings marks stay visible on miss instead of collapsing. Provider editor titles are a short Edit/Add verb before the identity chip. Live browser-tab favIconUrl observation, host peeling for chat., SVG site probes, and Google-before-DuckDuckGo generic-miss walking join that same favicon primitive. Favicon v6 stores quality-tagged blobs so guessed raster bytes never first-paint on Settings remount, discover runs once per peeled host, and declared dark/SVG icons beat oversized or tiny /favicon.ico. Remount first-paint now reuses any accepted peeled-host blob including ok-raster so Settings tabs share cache without refetching. Encoded rasters fill the 128 canvas instead of padding 16–48px glyphs, and declared 192px icons outrank 32px tiles. Legacy v6 padded blobs are dropped on migrate. Favicon v8 paint src prefers peeled-host data blobs over live tab https so Settings remounts reuse L0/L1 without refetching. 16px inline data: SVG tiles are treated as placeholders so 0-0.pro keeps its real favicon.png instead of a spinner ring. The shared icon table now includes Lucide listOrdered, images, and cable marks for Platform Configuration Image Paste Strategy cells. Shared workspace-tab-fulltext now owns content-only idle signatures so existing scans skip Copy when a stored pair already covers the conversation unless the same-representation live fingerprint grew."
    },
    "background/service-worker.js": {
      "maxFiles": 70,
      "targetFiles": 40,
      "maxSourceBytes": 1002359,
      "targetSourceBytes": 360000,
      "reason": "The Chromium service-worker initial-static closure owns signed channel verification with pinned recovery keys, strict declarative schema and host policy, content-addressed component storage, anti-rollback watermarks, opt-in one-shot alarms, atomic apply/rollback and committed-reset cleanup journals, generation-aware snapshot coordination and exact content-registration/DNR compensation, alongside its authenticated Frame RPC, one-shot prompt-handoff ledger and adaptive payload cleanup, normalized preferred-model overlay options in the stored snapshot, marker-driven startup refresh of canonical content registrations, one-sided Notion frame-load URL attestation, Grok Cookie, debugger and Notion navigation safety runtimes, plus durable event-scoped classification, URL-authenticated opening leases, phased tab creation outside the workspace lock, remembered ChatClub-tab listing including empty live tabs, deferred current-tab close, close of other live ChatClub tabs without deleting Tabs memory, export and import of remembered Tabs, and focus of ChatClub workspace tabs; listing and claim honor the durable tab binding when chrome.tabs.url still shows a detached workspace hash after New Chat replaceState, and focusing a live ChatClub tab in another window moves it into the requesting window; the combined security boundary is held to this exact footprint, including the Manus Summary catalog entry in packaged official-rules baselines and stored-catalog gap-fill from packaged components when ChatClub-rules has not published the new key; isolated-world frame-binding relay uses Chromium callback sendMessage, surfaces in-frame errors, and matches Grok rid-only document URLs; remembered Tabs keep per-pane conversation hrefs when some frames report home, and Kagi /c/{id} is a conversation identity; titled New Chat desks stay in ChatClub Tabs after reload; registerWorkspaceSessionTab does not reattach a detached lagged hash onto a rebound binding; and claim, persist, and commit accept that lagged sender URL, with claim responses declaring reboundFromStaleUrl in the page contract; the workspace-session sender context reads the live #workspace= hash from the browser tab record instead of the load-time-frozen MessageSender.url, and tabs.onUpdated registration detaches a replaced binding as a remembered row so a second New Chat keeps the previous New Chat desk. The shared topic-title module now also derives a conversation title from a chat site's document title (brand and placeholder stripping) and the frame-command contract declares the read-only getConversationOpening Message Navigator probe. Stored API profiles now carry a model catalog and per-slot outbound model assignments through options normalization. Stored options now persist compact Settings click-reorder as boolean settingsClickReorderButtonsEnabled. Stored options now persist per-platform site icons as appIcons. API provider rows reuse the shared favicon primitive with skip-catalog href/logoUrl so vendor hosts do not inherit chat-app icons. The shared favicon service now ingests live tab.favIconUrl values from chrome.tabs."
    },
    "background/firefox-background.js": {
      "maxFiles": 73,
      "targetFiles": 43,
      "maxSourceBytes": 1802462,
      "targetSourceBytes": 900000,
      "reason": "The Firefox document-background entry packages the same signed official-rules verifier, strict declarative contracts, immutable component repository, anti-rollback state, manual atomic activation, crash and committed-reset cleanup recovery, compensated content-registration/DNR coordination, one-shot prompt-handoff ledger with adaptive payload cleanup and normalized preferred-model overlay options as Chromium, together with marker-driven startup refresh of canonical content registrations, generated content fallbacks including Claude iframe-compat (isInIframe store patch for page-declared cross-origin modulepreload) plus Kagi composer hydration with thread-search exclusion, secure Frame RPC, one-sided Notion frame-load URL attestation, site-native Storage Access, paired pointer/native Notion model activation including the explicit Older models submenu path, updated Notion dialog model-picker and Effort handling, typed Grok secondary fallback, fail-closed Delete behavior when trusted input is unavailable, and durable event-scoped classification, URL-authenticated opening leases, phased tab creation outside the workspace lock, remembered ChatClub-tab listing including empty live tabs, deferred current-tab close, close of other live ChatClub tabs without deleting Tabs memory, export and import of remembered Tabs, and focus of ChatClub workspace tabs; listing and claim honor the durable tab binding when chrome.tabs.url still shows a detached workspace hash after New Chat replaceState, and focusing a live ChatClub tab in another window moves it into the requesting window; its larger exact static cost is tracked separately and now includes the Manus Summary userscript in generated content fallbacks plus stored-catalog gap-fill from packaged components; isolated-world frame-binding relay uses callback sendMessage on Chromium, surfaces in-frame errors, and matches Grok rid-only document URLs; generated content fallbacks include broader composer-stop generating detection for long Notion replies plus live Loading-web-page research status so idle extraction does not click Copy during Fable page reads; remembered Tabs keep per-pane conversation hrefs when some frames report home, and Kagi /c/{id} is a conversation identity; titled New Chat desks stay in ChatClub Tabs after reload; registerWorkspaceSessionTab does not reattach a detached lagged hash onto a rebound binding; and claim, persist, and commit accept that lagged sender URL, with claim responses declaring reboundFromStaleUrl in the page contract; the workspace-session sender context reads the live #workspace= hash from the browser tab record instead of the load-time-frozen MessageSender.url, and tabs.onUpdated registration detaches a replaced binding as a remembered row so a second New Chat keeps the previous New Chat desk. The shared topic-title module now also derives a conversation title from a chat site's document title (brand and placeholder stripping), the frame-command contract declares the read-only getConversationOpening Message Navigator probe, and the generated content fallbacks carry that probe in the Message Navigator bundle. Stored API profiles now carry a model catalog and per-slot outbound model assignments through options normalization. Stored options now persist compact Settings click-reorder as boolean settingsClickReorderButtonsEnabled. Stored options now persist per-platform site icons as appIcons. API provider rows reuse the shared favicon primitive with skip-catalog href/logoUrl so vendor hosts do not inherit chat-app icons. The shared favicon service now ingests live tab.favIconUrl values from chrome.tabs. Generated content fallbacks now fingerprint classified turns after stripping copy and toolbar chrome, without page-line fallback for idle identity."
    }
  },
  "lazyBoundaries": {
    "app/official-rules/service.js": {
      "owner": "app/runtime.js",
      "maxFiles": 1,
      "targetFiles": 0,
      "maxSourceBytes": 8851,
      "targetSourceBytes": 7000,
      "reason": "The Settings-owned official-rules service remains an explicit lazy boundary for signed catalog status, checks, atomic activation, rollback and user override coordination."
    },
    "app/history/controller.js": {
      "owner": "app/runtime.js",
      "maxFiles": 4,
      "targetFiles": 1,
      "maxSourceBytes": 69564,
      "targetSourceBytes": 16000,
      "reason": "Prompt History remains an explicit runtime-owned lazy controller; the viewer now includes a Pocket-style sidebar, save-to-Pocket action, saved-only Pocket badge, stored, live, or Pocket conversation turns with NFKC-tolerant matching, a selected-prompt modal title, persist-driven redraw of an already-open panel, and distinct loading versus unmatched conversation copy, and this budget tracks only modules added beyond the application initial-static closure; workspace-save injects the captured live turns and selects the matching prompt so a right-click refresh is visible in a closed or already-open viewer; item delete now uses an undo toast instead of a confirmation; the History viewer uses Pocket-aligned header columns, titlebar search, site favicons via the shared favicon primitive instead of chrome _favicon_ placeholders, merged USER clusters, Summary markdown rendering, copy and open-chat card actions, and shared ui/viewer-window.js focus/fullscreen/resize/restore chrome; sidebar titles use the full row, favicons sit left of the right-aligned timestamp with the Pocket action, and source counts appear only while the favicon stack is hovered; Tabs search now opens this same viewer pinned to one workspaceId so leftover Full text rows preview that chat instead of exploding pair cards. History logos use the shared favicon primitive instead of a private Google s2 helper. History candidate walking now includes live tab favicons and site ico/svg probes. History now writes decoded favicon blobs through the shared rememberDecoded seam. The titlebar search input stays mounted across query redraws so typing does not remount the field. A chat-frame load pins that field for a 150ms/1s retry window when it is focused or the query is nonempty, including delayed iframe autofocus, and does not let frame restore steal the caret."
    },
    "app/pocket/controller.js": {
      "owner": "app/runtime.js",
      "maxFiles": 4,
      "targetFiles": 2,
      "maxSourceBytes": 55568,
      "targetSourceBytes": 40000,
      "reason": "Pocket remains an explicit runtime-owned lazy controller with a switchable star/Pocket identity icon, a full-width sidebar title, favicons on the left of a right-aligned timestamp row, and source counts that appear only while the favicon stack is hovered, and this budget tracks only modules added beyond the application initial-static closure, including the shared ui/viewer-window.js focus/fullscreen/resize/restore primitive also used by History. Pocket entry logos walk the shared favicon candidate chain."
    },
    "app/prompt-focus/controller.js": {
      "owner": "app/runtime.js",
      "maxFiles": 1,
      "targetFiles": 0,
      "maxSourceBytes": 4780,
      "targetSourceBytes": 3200,
      "reason": "The initial workspace prompt-focus guard stays behind a runtime-owned lazy boundary so loading isolation does not enlarge the application bootstrap closure; a prompt or iframe pointer ends that lock, iframe focusin does not steal Clipboard writes or carets from the embedded document, and iframe load does not restore the prompt while a typed modal is open."
    },
    "app/settings/controller.js": {
      "owner": "app/runtime.js",
      "maxFiles": 30,
      "targetFiles": 18,
      "maxSourceBytes": 461473,
      "targetSourceBytes": 250000,
      "reason": "Settings remains an explicit runtime-owned lazy controller; its exact incremental closure includes background-owned import/reset coordination with durable committed-reset cleanup warnings, signed official-rules consent and status composition with cached-card language synchronization, apply-all and rollback entry points, sparse override clearing, platform and iframe source ordering, model controls, custom Summary/Delete permission notices, accessible General, Color and Overlays Workspace subtabs with pending-color retention, nested Tab Group, right-click menu, and ChatClub Tabs hover-button Fixed/More/Hidden settings with independent drag ordering and visibility, preferred-model overlay enablement and opacity with overlay-only synchronization, the focused topbar input setting, prompt-history date grouping, remembered ChatClub Tabs export/import, the ChatClub Tabs sidebar shortcut, header-mounted shortcut-settings and Prompt History search, the extracted Prompt History grouping model shared with the topbar History viewer including NFKC-tolerant conversation matching, Pocket-style USER clustering, and accessible viewport-filling window controls; English UI labels DouBao as 豆包（doubao） and Qianwen as 千问（qianwen）; leftover Settings window.confirm deletes now use themed confirmationModal via openConfirmationAction; import is an editor overlay, full reset requires acknowledgement, official-rules mutations use openConfirmationAction tones with applying busy copy, and Prompt History item plus placeholder deletes use undo toasts. Current models assignment selects and API provider model-catalog editing join that Settings lazy closure. Provider catalog rows now use a compact list surface; Current models uses a three-column Feature | Provider | Model header bar with hugging assignment rows instead of a four-column spreadsheet. A single-model catalog hides fallback and favorite actions. Star pins a model to the top of Current models dropdowns; a shield marks the fallback used when a slot does not pick one. Provider rows list every catalog model as chips when space allows. Catalog row actions fold into a vertical more menu. Appearance General compact click-reorder hides Settings list Move up / Move down by default until the reorder cluster is focused or activated. Site icons add app/settings/app-icon.js so platform rows share one favicon primitive and editorModal override UI. Settings and Apps controller contracts now accept optional faviconPort so runtime can wire the shared service without an extra-field throw. Models, Summary collectors, Message Navigation, and Topic Deletion rows now render the same display-only site mark so Platform Configuration icon overrides propagate. API Provider list marks use the same display primitive with skip-catalog and profile-owned logoUrl edited in the existing profile modal. The provider editor shows one clickable header mark that opens a nested compact icon editorModal with a preview rail, hugging URL/upload stack, Refresh, and Restore, live-applies draft logoUrl until parent Save, and top-aligns the Models column so stretched two-column height does not gap the catalog. Pending and missed Settings marks use a circular fill without an inset stroke so they do not read as checkboxes. Settings marks walk live tab favicons before guessed /favicon.ico. Settings provider marks prefer registerUrl, keep skip-catalog letter fallback, and persist quality-tagged favicon blobs after discover. Discover writeback applies a cached data blob onto the still-mounted Settings mark so tab switches share L1. Apps list marks now share the same discover writeback, pending CSS flashes only while src is remote, and v8 storage keeps accepted blobs without https pointers. Summary Record full text is a preference-row toggle without a nested Enabled well. Preferred models drop the four-column comparison table for per-platform cards with a wide two-zone fields body. Appearance Overlays groups Loading and Model on hugging compact slider rows instead of full-bleed stacked fields, folds field help into a ghost info trigger beside the title, gives both rows a left-side switch, and left-aligns titles and slider tracks on one subgrid. Appearance Topbar Input font-size uses that same hug-row without a switch. Appearance Color uses a hugging title+help | picker+hex row, drops the duplicate Accent subtitle and far-right preview orb. Appearance Site Toast folds duration into a hugging title+select row beside the 9:16 preview instead of a full-bleed duration card. Platform Configuration Image Paste Strategy cells render Lucide listOrdered, images, and cable marks with tooltip labels in a 52px icon column, while details and the custom editor keep text. The site icon editorModal is a compact preview rail beside a hugging URL/upload stack, with the format/size help folded into a ghost question-mark tooltip on the Image URL label, Upload/Refresh/Restore as one labeled action row, and Cancel/Save in the footer."
    },
    "app/share/controller.js": {
      "owner": "app/runtime.js",
      "maxFiles": 3,
      "targetFiles": 2,
      "maxSourceBytes": 48679,
      "targetSourceBytes": 40000,
      "reason": "Share remains an explicit runtime-owned lazy controller for image/text iframe capture, metric stitch, horizontal multi-frame layout, and Summary-backed text collection, and this budget tracks only modules added beyond the application initial-static closure."
    },
    "app/summary/controller.js": {
      "owner": "app/runtime.js",
      "maxFiles": 4,
      "targetFiles": 2,
      "maxSourceBytes": 82983,
      "targetSourceBytes": 48000,
      "reason": "Summary remains an explicit runtime-owned lazy controller and this budget tracks only modules added beyond the application initial-static closure, including per-frame idle full-text capture after send or workspace restore, a generating flag that blocks collect while a reply is still streaming, an unmatched collect that keeps waiting through a long reply instead of burning attempts, a longer generating wall so Fable-class thinking can finish, fail-closed per-iframe collection so one throwing frame cannot discard the others, persist of History live collects when Record Full Text is on, the Manus Summary catalog and source label, skip of a second Copy extract for an already-saved idle snapshot, sticky idle after a finished fingerprint so a later probe timeout cannot block Notion persist, and empty-home Summarize disable so start pages do not collect skipped blank chats. Summary source logos use the shared favicon renderer instead of a private Google s2 URL. Existing idle capture hydrates from workspaceTabFullText, treats a stored pair as coverage unless the same-representation live conversation grew, and waits out a home href instead of Copying."
    },
    "app/workspace/tab-search-controller.js": {
      "owner": "app/runtime.js",
      "maxFiles": 2,
      "targetFiles": 1,
      "maxSourceBytes": 36985,
      "targetSourceBytes": 16000,
      "reason": "Tabs search is a runtime-owned lazy viewerModal sibling of History and Pocket; it uses shared ui/viewer-window.js fullscreen/resize/restore chrome and Pocket entry/turn clusters for in-modal previews, while topbar Search must not open the ChatClub Tabs sidebar. This budget tracks only modules added beyond the application initial-static closure: the search controller plus viewer-window. Empty query lists recency groups, a query matches title plus recorded full text as one row per workspaceId, row click previews, and Open tab then closes. The titlebar search input stays mounted across query redraws so typing does not remount the field. A chat-frame load pins that field for a 150ms/1s retry window when it is focused or the query is nonempty, including delayed iframe autofocus. IME composition, Esc-clears, leftover forgotten full-text rows, and Record Full Text remain on the existing store."
    }
  }
}
