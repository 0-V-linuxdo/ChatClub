{
  "app/preferred-model/controller.js": {
    "maxLines": 1567,
    "target": 1200,
    "maxBytes": 64801,
    "targetBytes": 50000,
    "reason": "The preferred-model controller keeps one document-bound apply record across an explicitly unavailable primary or typed closed-menu Notion settlement failure and its optional secondary attempt, including fresh run correlation, navigation-generation bridge recovery, delivery-aware retry safety, queued-send readiness, failure policy handoff, per-attempt Gemini and Notion desired state, per-frame overlay and progress-Toast suppression synchronization, same-conversation Grok and Notion response URL changes, submission-owned Grok conversation creation routing, session-scoped five-minute secondary fallback memory, pre-record overlay target resolution, and a bootstrap gate that defers lifecycle-triggered applies until restored frames settle."
  },
  "content-src/capabilities/send-runtime.js": {
    "maxLines": 1215,
    "target": 1200,
    "maxBytes": 55140,
    "targetBytes": 50000,
    "reason": "The send runtime keeps exact-once composer delivery, typed image hydration, site-specific submit preparation, and the send-owned navigation correlation needed to keep Grok conversation creation from restarting preferred-model selection after the message is delivered."
  },
  "app/composer/controller.js": {
    "maxLines": 1594,
    "target": 1200,
    "maxBytes": 60978,
    "targetBytes": 45000,
    "reason": "The Composer controller owns immutable text-and-image draft revisions, revision-conditional clearing, history de-duplication, and the shared document-bound admission queue used by both ordinary Send and one-shot new-workspace prompt handoff so delivery uncertainty and preferred-model preparation cannot diverge; prompt-actions popover chrome now uses the shared overlay-surface contract with aria-haspopup/aria-expanded, a named linear menu, and Tab dismiss; empty collapsed placeholder clicks keep the native caret so the first load click is not swallowed; prompt expand skips while a typed modal owns the caret; a focused `.prompt-input` claims the overlay-grade composer caret owner (`composer: true`, `mode: overlay`, `composerInert`) so iframe autofocus cannot collapse the field mid-keystroke; center placement reparents the same `.prompt-shell` onto `#composer-center-host.overlay-surface` without a fifth overlay family; composer search mode attaches an overlay-surface result list under `.prompt-shell` through `search-panel.js` so Tabs search does not wrap the input in a typed modal."
  },
  "app/workspace/frame-controller.js": {
    "maxLines": 1303,
    "target": 1200,
    "maxBytes": 51650,
    "targetBytes": 45000,
    "reason": "The workspace frame controller keeps navigation-generation isolation, frame-load preparation, authenticated frame lifecycle reporting, target-correlated loading masks, prompt-focus restoration, and one post-layout navigation frame together so a delayed iframe load cannot publish stale state, size an embedded responsive page against a pre-layout viewport, or steal an input focus that was active before navigation. Typed modals now block that prompt restore the same way workspace popovers already did, keep the iframe inert when a load finishes while a modal is open, skip the navigation focus guard while a modal is open, and re-check `.modal` on every restore retry so a still-loading iframe cannot yank the caret out of Tabs search or History. Armed prompt restore yields to `pinOverlaySearchCaret` when the overlay-grade composer caret owner or leftover page caret owner already holds `.prompt-input`. Completing a chat-frame load pins a claimed composer or page caret owner without an armed restore generation and stamps a child-document page-caret lease onto `iframe.name` before src so MAIN-world autofocus cannot steal the top prompt; name stamping, prepare, and post-load refresh live in `app/workspace/page-caret-lease.js`. The Cookie preflight host list includes the exact Manus origin alongside Grok and Grok Mirror. New Chat now freezes the current conversation workspace before in-place navigation so the original tab remains reopenable, marks the resetting frame as having released its conversation before the first await, and clears that marker only when the new-topic home document loads or another ChatClub navigation supersedes it, so the rebound desk never records the frozen thread from a stale iframe src. In-group topic deletion now opens the themed confirmationModal via openConfirmationAction instead of window.confirm so popovers cannot cover the alertdialog, and that confirm now requires acknowledgement plus applying busy copy. Tab favicons resolve through effective(..., { appId }) so user overrides outrank discovered logos, and Google/DuckDuckGo URLs are never remembered."
  },
  "content-src/capabilities/preferred-notion-deepseek.js": {
    "maxLines": 1232,
    "target": 1200,
    "maxBytes": 51437,
    "targetBytes": 50000,
    "reason": "The Notion preferred-model capability keeps exact trigger scoping, model identity checks, explicit unavailable handling, nested Older models expansion, Effort application, Sources coordination, and post-selection settlement together so a model-specific fallback cannot activate an ambiguous control or report success before the live picker settles."
  },
  "background/grok-cookie-runtime.js": {
    "maxLines": 1749,
    "target": 1200,
    "maxBytes": 67086,
    "targetBytes": 50000,
    "reason": "The Grok Cookie runtime co-locates exact registered-frame authorization, browser-specific partition discovery, parent-document-bound Mirror account-switch capture and recovery including Arc's omitted top-frame metadata fallback, a connect-only same-document gateway WebSocket readiness proof with bounded a=1/a=2/a=3 account rotation, Cookie-ledger cleanup, first-party and profile URL mapping for Grok, Grok Mirror, and Manus, and fail-closed navigation handling so their shared attempt, frame, and top-document identities cannot drift across modules."
  },
  "background/official-rules-runtime.js": {
    "maxLines": 1338,
    "target": 1200,
    "maxBytes": 58696,
    "targetBytes": 50000,
    "reason": "The official-rules runtime is the single background configuration writer and keeps revision-CAS patch/import/reset handling, v3-to-v4 migration, immutable snapshot composition, signed component activation, rollback pins, alias authorization, reset cleanup journaling and startup recovery behind one serialized coordinator so atomic commit, compensation and destructive-operation fail-closed state cannot diverge across modules."
  },
  "app/runtime.js": {
    "maxLines": 1534,
    "target": 1200,
    "maxBytes": 66985,
    "targetBytes": 50000,
    "reason": "The application assembly root now also wires topic-title generation after the first composer send, the page-local ChatClub-tab sidebar toggle including its Ctrl/Cmd+B shortcut, remembered-tab refresh after hydration, Settings-owned Tabs export/import callbacks, per-frame idle full-text capture after send and after workspace restore, persist-driven redraw of an already-open History viewer, the topbar Search action that lazily opens the Tabs search viewer, Appearance options for ChatClub Tabs hover-button placement, and an inert workspace-session port for Settings beside the existing Composer, Topbar, workspace-session, and overlay-aware Escape dismissal composition; Share panel open/sync/Escape/fullscreen wiring joins the existing lazy Summary/Pocket assembly without pulling Share modules into the bootstrap graph; the topbar History action lazily opens the Prompt History viewer without pulling History modules into the bootstrap graph; History save-to-Pocket uses runtime-owned Pocket and conversation ports so the History lazy boundary does not import those controllers; ChatClub Tabs and History now receive favicon and Pocket save ports so sidebar rows can show chat site icons and one-click Pocket without importing those lazy controllers into bootstrap; right-clicking the visible or folded Pocket/History topbar buttons immediately captures the current workspace and saves it without opening the panel; Chinese Kimi host inference now labels the site Kimi.com so it stays distinct from Kimi.ai; Qwen.ai stays international while Qianwen.com is inferred as \u5343\u95ee; English UI labels DouBao as \u8c46\u5305\uff08doubao\uff09 and Qianwen as \u5343\u95ee\uff08qianwen\uff09 because those products are China-only; History right-click now loads the lazy viewer to inject the captured turns and select the matching prompt instead of only refreshing an already-open panel; New Chat All freezes the current conversation workspace once before resetting frames and hands the leaving iframes to preserve so the rebound desk is recorded at home; Delete All Topics now uses themed confirmationModal via openConfirmationAction instead of window.confirm, with acknowledgement and applying busy copy; ChatClub Tabs search now enters composer search mode on `.prompt-shell` while the viewerModal remains a lazy deep link; hiding or unloading the ChatClub page flushes the workspace snapshot so Tabs survive an extension reload. Desk auto-naming: the assembly root builds the workspace auto-title controller beside topic-title generation, feeds it frame location and load-finished lifecycle events, exposes the Message Navigator site config lookup as its read-only probe source, and names an untitled desk after a Pocket batch restore from the saved question. Empty-home Delete All Topics now reads workspace.hasDeletableActiveThreads through the bound workspace port so the topbar trash disables instead of toasting skipped chats. Empty-home Summary now reads workspace.hasSummarizableActiveThreads the same way so the topbar Summary control disables instead of opening a collector on Grok/Kagi/ChatGPT/Gemini homepages. Empty-home Share now reads workspace.hasShareableActiveThreads so the topbar Share control disables instead of opening a capture panel on those same start pages. Empty-home New Chat All now reads workspace.hasNewChatActiveThreads so the topbar New Chat control disables instead of freezing a home-only desk. Compact click-reorder is stamped as html dataset.settingsClickReorder from settingsClickReorderButtonsEnabled in applyTheme so Settings list first columns hide Move up / Move down by default. Settings receives the shared favicon service as faviconPort so platform icon overrides and auto-fetch stay on one primitive. Live browser-tab favIconUrl observation now feeds that same service so an open site's Chrome-resolved icon outranks guessed /favicon.ico. Existing idle Record Full Text hydrates saved signatures from workspaceTabFullText and skips Copy unless live turn count or tail hash grew. Composer search rows now receive site favicon stacks through a runtime renderFavicons port instead of mapping appIds to display names."
  },
  "app/workspace/tabs-sidebar-controller.js": {
    "maxLines": 1662,
    "target": 1200,
    "maxBytes": 59272,
    "targetBytes": 40000,
    "reason": "The remembered ChatClub-tab sidebar owns live/closed partitioning, last-viewed/edited/created plus name/open sorting with prompt-history date groups, folder create/drag membership, drag reorder with clickable Move up / Move down, pin order, overlay rename/delete, Appearance-owned hover-button Fixed/More/Hidden folding with an owner-scoped three-dot popover, close-others cleanup, the local sidebar Search field, stacked chat favicons from remembered app IDs, and one-click Pocket from stored or live conversation turns; sort and More menus are named and Tab-dismiss; the sidebar search field stays a local find-in-list while topbar Search opens the lazy Tabs search viewer; a stale live tabId now falls back to opening the workspace instead of requiring a second click; a live New Chat rebound must not reuse the previous conversation name from the browser tab title; the current live row uses the in-page workspace apps so an empty New Chat is not labeled ChatClub 1. The Pocket batch-restore layout name (Restore group / \u4e00\u952e\u6062\u590d) is a produced-by label, not a row name, so a preserved untitled desk falls back to its chat apps."
  },
  "shared/storage-schema.js": {
    "maxLines": 1450,
    "target": 1200,
    "maxBytes": 58763,
    "targetBytes": 50000,
    "reason": "The configuration schema owns custom-platform URL https coercion and domain-catalog draft suggestions together with options normalization, dehydration, built-in catalog merges, and retired two-state tooltip-id aliases so imported and editor-authored custom apps cannot drift; Chinese Kimi host inference now labels the site Kimi.com so it stays distinct from Kimi.ai; Qwen.ai stays international while Qianwen.com is inferred as \u5343\u95ee; a one-shot History topbar migration appends the viewer button after Pocket unless the user already hid it; Pocket identity now normalizes the star/Pocket icon preference; API providers now persist a model catalog, favorite pin list, and per-slot outbound model assignments. Settings compact click-reorder persists as boolean settingsClickReorderButtonsEnabled. Options now persist per-platform site icon overrides as appIcons (HTTPS URL or data:image, 64 KB cap) through the same schema as other Settings options. API provider profiles may persist an optional logoUrl (HTTPS or data:image, same 64 KB cap) without writing site appIcons."
  },
  "shared/i18n.js": {
    "maxLines": 2899,
    "target": 1500,
    "maxBytes": 169884,
    "targetBytes": 82000,
    "reason": "Bilingual translation catalog data is kept co-located for key parity; the signed official-rules update-management and component-status views, consent, check, apply-all, compatibility, rollback, per-component source, confirmation and accessibility copy join the committed-reset cleanup warning, queued-send, preferred-model auto-selection overlay settings plus structured model, thinking, All Sources and Effort attempt labels, Workspace appearance subtabs, nested Tab Group right-click menu and ChatClub Tabs hover-button placement settings, prompt-handoff, platform-management labels including custom-platform domain autofill, iframe tab context-menu labels, prompt-history date-group labels, remembered ChatClub-tab sidebar labels including tab count, close-others cleanup, pin/unpin, More, sort modes, folder create/rename/delete, date-group headings and resize copy, Tabs export/import copy, topic-title edit copy, the ChatClub Tabs sidebar shortcut, shortcut-settings search copy, and Prompt History search, loading, and unmatched-conversation copy under exact line and byte ratchets; Share panel and shortcut copy joins the bilingual catalog; the topbar History button and openHistoryPanel shortcut copy join the bilingual catalog; right-click Pocket/History workspace-save toasts join the bilingual catalog; English UI labels DouBao as \u8c46\u5305\uff08doubao\uff09 and Qianwen as \u5343\u95ee\uff08qianwen\uff09; Pocket sidebar and focus-mode tooltip rows share one settings target each; Chinese Pocket chrome defaults to \u6536\u85cf with a switchable star/Pocket icon; History workspace-save distinguishes added versus refreshed prompts; Move up / Move down labels cover WCAG 2.5.7 click reorder; leftover window.confirm replacements add themed confirmation titles for custom platforms, API profiles, templates, collectors, navigator/delete sites, prompts, history, placeholders, and topic deletion; confirmation copy now names the object in the title, keeps irreversibility for danger/warning, and topic delete requires acknowledgement; reversible history and placeholder deletes use undo toasts; the model-inventory catalog names outbound API slots, iframe preferred models, and the relabeled iframe preferences section; Current models assignment and API provider model-catalog copy join that same inventory catalog, including the API-key reveal labels and the catalog fallback-model hint. Appearance compact click-reorder switch copy joins the bilingual catalog. Site-icon editor, change-icon, refresh, restore, and save/restore toasts join the bilingual catalog."
  },
  "tools/browser-smoke.mjs": {
    "maxLines": 1440,
    "target": 1200,
    "maxBytes": 75294,
    "targetBytes": 60000,
    "reason": "The cross-engine smoke exercises protected-header initial loading, target-correlated workspace-frame reset that ignores delayed initial-navigation events before retention baselines, owner-tab session rules, durable workspace reconstruction with failure-shell diagnostics, document-initiated iframe self-navigation, the authenticated binding handshake, explicit importability of every app lazy boundary including the signed official-rules service and the Prompt History viewer, complete Settings-menu and fullscreen-window probes, preferred-model overlay sizing and opacity across themes and narrow panels, accessible Workspace subtabs with focus and pending-color retention, content-sized Apps headers, source-specific iframe list drag-handle shapes without a redundant source column, compact iframe hover-help validation, and the one-shot prompt-handoff tab lifecycle with duplicate-click and cleanup assertions. Custom-app deletion now confirms through the themed alertdialog instead of mocking window.confirm."
  },
  "app/summary/controller.js": {
    "maxLines": 1215,
    "target": 1200,
    "maxBytes": 54193,
    "targetBytes": 48000,
    "reason": "The Summary controller now hydrates existing idle full-text capture from persisted workspaceTabFullText frames, keys snapshots by instanceId and conversation href, and still owns per-frame Last-N Copy collection plus the send-kind idle path."
  },
  "content-src/shared/summary-runtime.js": {
    "maxLines": 1232,
    "target": 1200,
    "maxBytes": 49374,
    "targetBytes": 45000,
    "reason": "Conversation fingerprints keep Notion chat ?t= on href so idle existing scans match stored conversation identity, and still hash classified user and assistant turn text after stripping copy, toolbar, and button chrome."
  }
}
